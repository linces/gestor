program srvTLockAppRm;

{$APPTYPE CONSOLE}

{$R *.res}

uses
  System.SysUtils,
  System.Json, Utils.DataSet.JSON.Helper, Horse.Jhonson, Horse.Compression, REST.Types,
  REST.Client, REST.Response.Adapter, Horse.Cors, Horse.Paginate, Horse.Utils.ClientIP;

begin
  try
    { TODO -oUser -cConsole Main : Insert code here }
  except
    on E: Exception do
      Writeln(E.ClassName, ': ', E.Message);
  end;
end.
