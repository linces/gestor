program Comandas;

uses
  System.StartUpCopy,
  FMX.Forms,
  unLogin in 'unLogin.pas' {Form3};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm3, Form3);
  Application.Run;
end.
